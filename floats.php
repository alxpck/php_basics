<?php

$num_three = 3;
$distance_home = 1.2;
$distance_work = 2.5;

var_dump($distance_home + $distance_work + $num_three);

/* 
when you use a float inside var_dump it returns
float(x) where x is the value of the float

?>