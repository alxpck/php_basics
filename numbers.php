<?php

$num_one = 1;
$num_two = 2;

var_dump($num_one);
var_dump(1);
var_dump("123");
var_dump($num_one + $num_two);

// var_dump is a function that will check the type of a variable. It also prints the result to the console.

/* 

if the variable is an integer it will return
int(x) where x is the value of the integer

if the variable is a string it will return
int(x) "string" where x is the length of the string and "string" displays the contents of the string


?>